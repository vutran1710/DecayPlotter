import {
  Jumbotron,
} from 'inferno-bootstrap'

const Header = props => (
  <Jumbotron>
    <h1 className="display-3">Plotting Area</h1>
    <p className="lead">This is a simple plotting app, as demo for how a Exponential Decay mechanism works on fundamental of Math</p>
    <div>
      <title>Change elapsed time</title>
    </div>
  </Jumbotron>
)

export default Header
